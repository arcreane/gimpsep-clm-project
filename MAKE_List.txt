src/main/main.cpp


LINUX :

CC = g++
CFLAGS = -Wall -std=c++11

SRCDIR = src
BINDIR = bin
OBJDIR = obj
OPENCV_INCLUDE = -I/usr/include/opencv4
OPENCV_LIBS = -L/usr/lib/x86_64-linux-gnu -l:libopencv_core.so -l:libopencv_highgui.so -l:libopencv_imgcodecs.so -l:libopencv_imgproc.so -l:libopencv_videoio.so

all: $(BINDIR)/main 

$(BINDIR)/main: $(OBJDIR)/main.o
	@mkdir -p $(BINDIR)
	$(CC) -o $@ $^ $(OPENCV_LIBS)

$(OBJDIR)/main.o : $(SRCDIR)/main/main.cpp
	@mkdir -p $(OBJDIR)
	$(CC) $(CFLAGS) -c $< -o $@ $(OPENCV_INCLUDE)

clean: 
	@rm -rf $(OBJDIR) $(BINDIR)
	
	
	

CLION :

cmake_minimum_required(VERSION 3.27)
project(IG_2409_Project)

set(CMAKE_CXX_STANDARD 14)
set(ENV{OPENCV_DIR} "C:\\tools\\opencv\\build")
set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${CMAKE_SOURCE_DIR}/cmake/")

find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRST})

add_executable(IG_2409_Project
        src/main/main.cpp
	# PUT ALL LIGNE IN THE TOP OF THE FILE
)

set(OpenCV_LIBS opencv_core opencv_imgproc opencv_highgui opencv_imgcodecs)
target_link_libraries(IG_2409_Project ${OpenCV_LIBS})
